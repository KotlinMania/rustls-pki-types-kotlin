# Port targets — `rustls-pki-types-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `rustls-pki-types` that codex-rs uses, and therefore the minimum subset that `rustls-pki-types-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-124] (rank `29`, tier `direct`, status `To Do`)

**Cargo package name**: `rustls-pki-types`
**Rust module name (in `use` statements)**: `rustls_pki_types`

## Summary

- **5** distinct symbols imported by codex-rs
- **6** total `use` statements
- **2** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `rustls_pki_types::CertificateDer`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/src/custom_ca.rs:53` (`codex-client`) — `use rustls_pki_types::CertificateDer`
- `codex-client/tests/ca_env.rs:22` (`codex-client`) — `use rustls_pki_types::CertificateDer`

### `rustls_pki_types::PrivateKeyDer`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/tests/ca_env.rs:23` (`codex-client`) — `use rustls_pki_types::PrivateKeyDer`

### `rustls_pki_types::pem::PemObject`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/src/custom_ca.rs:54` (`codex-client`) — `use rustls_pki_types::pem::PemObject`

### `rustls_pki_types::pem::SectionKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/src/custom_ca.rs:55` (`codex-client`) — `use rustls_pki_types::pem::SectionKind`

### `rustls_pki_types::pem`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-client`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `codex-client/src/custom_ca.rs:56` (`codex-client`) — `use rustls_pki_types::pem`
